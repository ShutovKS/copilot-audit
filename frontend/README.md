# TestOps Forge Frontend

![React](https://img.shields.io/badge/React-19-blue) ![Vite](https://img.shields.io/badge/Vite-7-purple) ![TypeScript](https://img.shields.io/badge/TypeScript-5-blue) ![Tailwind](https://img.shields.io/badge/Tailwind-4-cyan) ![Zustand](https://img.shields.io/badge/State-Zustand-orange)

**TestOps Forge Frontend** — это продвинутая IDE-консоль для управления мультиагентной системой генерации автотестов. Приложение предоставляет эргономичный интерфейс для общения с AI-агентами, визуализации их "мыслительного процесса", редактирования кода и глубокой отладки (Playwright Traces).

Интерфейс выполнен в стилистике **Cyberpunk / Dark Console**, вдохновленной *Cloud.ru Console*, с акцентом на снижение визуального шума и максимальную производительность.

## 🚀 Ключевые Возможности

### 🤖 Agent Interaction & Workflow

* **Context-Aware Chat**: Чат с сохранением истории и поддержкой сессий.
* **AI Prompt Enhancer**: Встроенная функция (кнопка 🪄 в сайдбаре), которая использует LLM для улучшения и структурирования вашего запроса перед отправкой агенту.
* **Human-in-the-Loop (HITL)**: Критический этап контроля. Перед написанием кода агент Analyst предлагает **план тестирования**. Вы можете отредактировать план, утвердить его или вернуть на доработку через модальное окно `ApprovalModal`.
* **Visual Stepper & Logs**: Терминал визуализирует текущий этап пайплайна (Analyst → Coder → Reviewer) с цветовым кодированием логов в реальном времени.
* **Smart Context Injection**:
  * 📦 **ZIP-архивы**: Загрузка исходников для локального анализа структуры проекта.
  * 🐙 **Git Integration**: Прямой анализ репозиториев (Public/Private) с поддержкой токенов доступа.

### 💻 Code Editor & IDE Features

* **Monaco Editor**: Полноценный редактор с подсветкой синтаксиса (Python/Markdown), миникартой и темой *Cloud Rounded*.
* **Streaming Typing**: Эффект "живого печатания", отображающий код посимвольно по мере его генерации агентом.
* **Smart Tabs**: Мгновенное переключение между контекстами:
  * 🐍 `test_suite.py` — Сгенерированный код.
  * 📄 `test_plan.md` — Утвержденный план тестирования.
  * 📊 `Allure Report` — HTML-отчет о прогоне (появляется после запуска).
* **GitLab Integration**: Экспорт теста в один клик. Система создает ветку, коммит и **Merge Request** через GitLab API.

### ⚙️ Execution & Debugging

* **Remote Execution**: Асинхронный запуск тестов в изолированных Docker-контейнерах (DinD).
* **Live Logs Streaming**: Логи Pytest транслируются в терминал в реальном времени через SSE + Redis.
* **Embedded Reports**: Просмотр Allure-отчетов прямо внутри IDE (через iframe) без переключения вкладок браузера.
* **Auto-Fix Workflow**: При падении теста в редакторе появляется кнопка **Auto-Fix with AI**. Логи ошибки и контекст отправляются агенту Debugger для автоматического исправления.

### 🔍 Advanced Diagnostics

* **Trace Inspector**: Детальный разбор падений (доступен в истории запусков):
  * 📸 **DOM Snapshot**: HTML-код страницы в момент ошибки с подсветкой синтаксиса.
  * 💡 **Hypothesis**: Гипотеза агента о причине сбоя.
  * 🌐 **Network & Console**: Логи сетевых запросов и ошибки консоли браузера.
* **Error Recovery**: Модальное окно `ErrorSuggestionModal` предлагает стратегии восстановления при сбоях генерации (смена модели, упрощение промпта).
* **System Health**: Дашборд состояния всех микросервисов (Backend, DB, LLM Provider).

### 🧠 Multi-Model Support

Переключение LLM-провайдеров на лету в настройках:

* **Qwen 3 Coder / Next**: Рекомендованные модели.
* **GigaChat / T-Pro**: Российские модели (Sber, T-Tech).
* **OpenAI / MiniMax / Zhipu**: Альтернативные провайдеры для специфичных задач.

## 🛠 Технологический Стек

* **Core**: React 19, Vite 7, TypeScript 5.9
* **State Management**: Zustand 5 (Persist middleware для сохранения сессий в `localStorage`)
* **Styling**: Tailwind CSS v4 (Zero-config, CSS variables)
* **Components**:
  * `@monaco-editor/react` — ядро редактора кода.
  * `lucide-react` — иконографика.
  * `framer-motion` — плавные анимации интерфейса.
* **Networking**:
  * **Native Fetch (Streams API)** — для обработки SSE событий чата и логов.
  * **Axios** — для атомарных HTTP запросов.
* **Deploy**: Docker + Nginx (Alpine) для раздачи статики.

## 📂 Структура Проекта

```text
src/
├── pages/
│   └── Workspace.tsx       # Основной лейаут (Sidebar + Editor + Terminal)
├── widgets/                # Независимые UI-модули
│   ├── Terminal.tsx        # Логи, Stepper прогресса
│   ├── Editor.tsx          # Monaco Editor + Action Bar (Run/Export)
│   ├── Sidebar.tsx         # Чат, загрузка файлов, Prompt Enhancer
│   ├── HistoryList.tsx     # Список сессий и загрузка старых прогонов
│   ├── SettingsModal.tsx   # Выбор модели, Health Check, Session Key
│   ├── ApprovalModal.tsx   # Human-in-the-Loop интерфейс
│   └── DebugReportModal.tsx# Визуализация Trace Context
├── entities/
│   ├── store.ts            # Глобальный Store (объединяет слайсы)
│   └── slices/             # Логика (chat, ui, settings)
├── shared/
│   ├── api/                # SSE Stream Service и Axios клиент
│   └── lib/                # Утилиты (cn, tailwind-merge)
└── App.tsx                 # Глобальные провайдеры и модалки
```

## 🚀 Запуск и Разработка

### Полный запуск (Production Mode)

Используйте Docker Compose в корне репозитория для запуска всего стека (Frontend раздается через Nginx на 80 порту):

```bash
docker-compose up --build -d --force-recreate
```

Приложение будет доступно по адресу `http://localhost`.

### Локальная разработка (Frontend Only)

Для работы над UI без пересборки контейнеров (требуется запущенный бэкенд на `localhost:8000`):

1. **Установка зависимостей:**

    ```bash
    npm install
    ```

2. **Запуск Dev-сервера:**

    ```bash
    npm run dev
    ```

Приложение будет доступно по адресу `http://localhost:5173`.
*Vite Proxy уже настроен в `vite.config.ts` для проксирования запросов `/api` на бэкенд.*

## 🎨 Особенности UX

* **Session Portability**: В настройках доступен уникальный `Session UUID`. Вы можете скопировать его и ввести на другом устройстве, чтобы восстановить полный контекст работы и историю.
* **Adaptive Layout**: Flexbox-верстка на всю высоту окна браузера ("application-like" feeling), скроллятся только внутренние области контента.
* **Error Feedback**: Информативные Toast-уведомления и цветовая индикация состояний в терминале.
