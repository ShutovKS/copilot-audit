# TestOps Forge Frontend

![React](https://img.shields.io/badge/React-19-blue) ![Vite](https://img.shields.io/badge/Vite-7-purple) ![TypeScript](https://img.shields.io/badge/TypeScript-5-blue) ![Tailwind](https://img.shields.io/badge/Tailwind-4-cyan) ![Zustand](https://img.shields.io/badge/State-Zustand-orange)

**TestOps Forge Frontend** — это продвинутая IDE-подобная консоль для взаимодействия с мультиагентной системой генерации тестов. Приложение предоставляет интерфейс для общения с AI-агентами, визуализации процесса их "мышления", редактирования кода и глубокой отладки.

Интерфейс выполнен в **Cyberpunk / Dark Console** стилистике, вдохновленной **Cloud.ru Console**, с акцентом на эргономику и снижение визуального шума.

## 🚀 Ключевые Возможности

### 🤖 AI Agent Interaction

* **Чат с контекстом**: Общение с агентами (Analyst, Coder) с поддержкой сохранения истории сессий.
* **Streaming Logs (SSE)**: Визуализация работы агентов в реальном времени через терминал. Цветовое кодирование этапов (Analysis, Coding, Reviewing).
* **Context Injection**: Загрузка исходного кода через **ZIP-архивы** или прямые ссылки на **Git-репозитории** (автоматический анализ структуры проекта и эндпоинтов).
* **Smart Error Recovery**: При сбоях генерации система предлагает переключиться на более мощную модель или упростить промпт (через `ErrorSuggestionModal`).

### 🧠 Multi-Model Support

Гибкое переключение между LLM провайдерами прямо в интерфейсе:

* **Qwen 3 Coder / Next**: Основные модели для генерации кода.
* **GigaChat / T-Pro**: Поддержка российских моделей.
* **OpenAI / MiniMax / Zhipu**: Альтернативные провайдеры для сложных задач.
* *Статус моделей (Free/Paid) и размер контекста отображаются в настройках.*

### 💻 Code Editor & IDE Features

* **Monaco Editor**: Полноценный редактор кода с подсветкой синтаксиса Python/Markdown и кастомной темой.
* **Live Updates**: Код обновляется в редакторе посимвольно (stream) по мере того, как агент его пишет.
* **Test Plan Viewer**: Отдельная вкладка для просмотра сгенерированного Markdown тест-плана.
* **Local Parsing**: Автоматический скроллинг и фокус на активных файлах.

### ⚙️ Execution & Debugging

* **Run Tests**: Запуск сгенерированных тестов в изолированных Docker-контейнерах (через Backend API) **асинхронно, с потоковой передачей логов в реальном времени через SSE**.
* **Allure Reports**: Встроенный просмотр HTML-отчетов Allure (через iframe) без выхода из приложения, теперь отдаются напрямую Nginx, что устраняет проблемы с дублированием интерфейса.
* **Auto-Fix Workflow**: При падении теста появляется кнопка **Auto-Fix with AI**, которая отправляет логи и трейсы обратно агенту для исправления кода.
* **Debug Inspector**: Детальный отчет о падении теста (Trace Analysis):
  * 📸 Скриншот DOM-дерева в момент ошибки.
  * 🌐 Сетевые ошибки и статусы запросов.
  * 🖥 Логи консоли браузера.
  * 💡 Гипотеза агента о причине падения.

### 🛠 Tools & System Health

* **System Dashboard**: Мониторинг состояния Backend API, подключения к БД и доступности LLM провайдера в реальном времени.
* **GitLab Export**: Экспорт готового теста в GitLab Merge Request в один клик.
* **AI Enhance**: Улучшение промпта пользователя с помощью LLM перед отправкой ("Pre-flight check").
* **Session Management**: Полное восстановление контекста работы через уникальный `Session UUID`.

## 🛠 Технологический Стек

* **Framework**: React 19 + Vite 7
* **Language**: TypeScript 5.9
* **State Management**: Zustand 5 (с персистенцией в LocalStorage)
* **Styling**: Tailwind CSS v4 (новая версия без config-файла)
* **Components**:
  * `@monaco-editor/react` (Редактор кода)
  * `lucide-react` (Иконки)
  * `framer-motion` (Анимации переходов)
* **API Client**: Axios + Native Fetch (для обработки потоковых SSE данных)

## 📂 Структура Проекта

Проект организован модульно:

```text
src/
├── pages/          # Страницы приложения (основная: Workspace)
├── widgets/        # Крупные самостоятельные виджеты
│   ├── Terminal.tsx        # Логи, статусы этапов (Stepper)
│   ├── Editor.tsx          # Monaco Editor + панель действий (Run/Export)
│   ├── Sidebar.tsx         # Чат, загрузка файлов, настройки промпта
│   ├── SettingsModal.tsx   # Настройки, выбор модели, Health Check
│   └── DebugReportModal.tsx# Визуализация трейсов Playwright
├── entities/       # Бизнес-логика и состояние
│   └── store.ts    # Глобальный Zustand стор (SSE логика, управление стейтом)
├── shared/         # Общие утилиты и API клиент
│   ├── api/        # Axios инстанс и методы
│   └── lib/        # Хелперы (cn, utils)
└── App.tsx         # Корневой компонент и лейаут
```

## 🚀 Запуск и Разработка

Для запуска всего приложения, пожалуйста, обратитесь к инструкциям в корневом `README.md` (`docker-compose up --build -d`).

### Локальная разработка (только Frontend)

Если вы хотите работать только над фронтендом без запуска полного стека через Docker Compose:

1. **Предварительные требования:** Node.js 20+
2. **Установка зависимостей:**

    ```bash
    npm install
    ```

3. **Запуск Dev-сервера:**

    ```bash
    npm run dev
    ```

    Приложение будет доступно по адресу `http://localhost:5173`.
    *Примечание: Для корректной работы API локально настройте прокси в `vite.config.ts` или убедитесь, что бэкенд доступен.*

## 🎨 UI/UX Особенности

* **Адаптивный лейаут**: Flexbox верстка на всю высоту экрана, панели не скроллятся целиком, только их контент.
* **Темная тема**: Палитра `#131418` (Background) и `#1f2126` (Surface), акцентный цвет `#00b67a` (Evolution Green).
* **Feedback**: Toast-уведомления, скелетоны загрузки, анимации появления элементов терминала.
