# Руководство пользователя TestOps Evolution Forge

Добро пожаловать в **TestOps Evolution Forge** — автономную IDE для QA-инженеров на базе Cloud.ru Evolution. Этот гид поможет вам эффективно использовать возможности мульти-агентной системы для генерации, отладки и автономной поддержки автотестов.

## 🖥 Интерфейс "Dark Console"

Приложение спроектировано в стиле киберпанк-консоли для максимальной концентрации:

1. **Sidebar (слева):**
    * **Config:** Чат с AI, загрузка файлов (`.zip`), анализ Git-репозиториев.
    * **History:** Хронология всех сессий. Иконки ✅ (Успех) и 🐞 (Ошибка) показывают статус прогона.
2. **Workspace (центр):**
    * **Monaco Editor:** Редактор кода с подсветкой синтаксиса Python и эффектом "живого печатания".
    * **Smart Tabs:** Мгновенное переключение между контекстами:
        * `test_suite.py` — Сгенерированный код.
        * `test_plan.md` — Утвержденный план тестирования.
        * `Allure Report` — Встроенный HTML-отчет (появляется после запуска).
3. **Terminal (справа):**
    * **Visual Stepper:** Визуализация этапов пайплайна (Analyst → Coder → Reviewer).
    * **Live Logs:** Логи выполнения тестов транслируются в реальном времени.

---

## 🧠 1. Процесс Генерации (The Workflow)

### Шаг 1: Постановка задачи и AI Enhance

Просто напишите задачу в чат.

* **Пример:** `Протестируй корзину на https://example.com`
* **Совет:** Нажмите иконку **Волшебной палочки (🪄)** над полем ввода. Встроенный LLM-инженер перепишет ваш короткий запрос в структурированный промпт с шагами и требованиями.

### Шаг 2: Human-in-the-Loop (Контроль плана)

Система не пишет код вслепую. Агент-Аналитик сначала составляет **Тест-план**.

1. Процесс ставится на паузу, открывается окно **Approval Modal**.
2. **Ваши действия:**
    * ✏️ **Edit:** Вы можете переписать шаги теста прямо в окне, если агент что-то упустил.
    * ✅ **Approve:** Подтвердить план (передача управления Кодеру).
    * ❌ **Reject:** Отклонить план и уточнить задачу.

### Шаг 3: Генерация и Валидация (Reviewer Gate)

После написания кода в дело вступает агент **Reviewer**. Он проводит 3 проверки:

1. **Security Check:** Блокирует опасные импорты (`os`, `subprocess`).
2. **Linter (Ruff):** Форматирует код по PEP8.
3. **Locator Dry Run:** Агент **заходит на сайт** и проверяет, существуют ли найденные селекторы на странице. Если нет — код отправляется на переделку еще до того, как вы его увидите.

---

## 📚 2. Долгосрочная память (Memory)

Система учится на ошибках проекта.

* **Сохранение опыта:** Когда `Debugger` исправляет ошибку, он формулирует "Урок" (например, *"На этом проекте для клика по кнопке 'Купить' нужен force=True"*).
* **Применение:** При следующей генерации эти знания автоматически подтягиваются из векторной базы (ChromaDB), предотвращая повторение багов.

---

## 👁️ 3. Active Vision (Работа с UI)

Чтобы тесты были надежными:

1. **Web Inspector:** При наличии URL в запросе, система запускает headless-браузер.
2. **Semantic Parsing:** Мы не используем скриншоты. Мы парсим живой DOM, извлекая `data-testid`, `aria-label` и устойчивые классы. Это гарантирует точность селекторов.

---

## 📦 4. Работа с Репозиториями (White-Box)

Для интеграционных тестов системе нужно знать контекст вашего проекта.

### Способ А: ZIP Архив

Нажмите 📎 и загрузите архив с исходниками. Агент проиндексирует структуру файлов (Python/Java/JS).

### Способ Б: Git Agent (ReAct)

1. Нажмите иконку **Git Branch**.
2. Введите URL репозитория (поддерживаются приватные репо с токеном).
3. **ReAct Mode:** Включится агент-исследователь. Он будет использовать инструменты `read_file`, `search_code` и `ls`, чтобы самостоятельно найти Page Objects и API-клиенты в вашем коде и использовать их в тесте.

---

## 🔧 5. Запуск и Отладка

### Изолированный запуск

Нажмите **▶ Run Test**. Тест запустится в изолированном Docker-контейнере (DinD). Результаты (Pass/Fail) и ссылка на отчет появятся в терминале.

### 🛠 Auto-Fix with AI

Если тест упал, в редакторе появится кнопка **Auto-Fix**.

1. Нажмите её.
2. Система соберет "пакет улик" из `trace.zip`:
    * Логи консоли браузера.
    * Сетевые запросы (Network).
    * **DOM Snapshot** в момент ошибки.
3. **Debugger Agent** проанализирует это и перепишет код.

### 🕵️‍♂️ Debug Report

В истории запусков (слева) нажмите на иконку **Жука (🐞)** у упавшего теста. Откроется детальный отчет, где можно увидеть гипотезу агента о причине падения и состояние DOM-дерева.

---

## ⏰ 6. Автономное Обслуживание (Scheduler)

Система работает, пока вы отдыхаете.

* **Health Check:** Каждые **6 часов** фоновый планировщик запускает успешные тесты.
* **Auto-Healing:** Если тест, который раньше работал, начал падать (изменилась верстка), автоматически запускается процедура восстановления.
* **Уведомления:** Вы увидите сообщение в разделе уведомлений: *"Test was automatically repaired"*.

---

## ⚙️ 7. Настройки и Экспорт

### System Health

В настройках (⚙️) доступен дашборд состояния всех микросервисов: API, БД и LLM.

### Session Portability

Во вкладке **Account** находится ваш `Session UUID`. Сохраните его! Введя этот ключ на другом устройстве, вы полностью восстановите историю чата и настроек.

### Export to GitLab

Тест готов?

1. Нажмите **GitMerge (Export)**.
2. Введите Project ID и Token.
3. Система создаст ветку и Merge Request через GitLab API.

---

## 🆘 Troubleshooting

Если генерация зависла или выдала ошибку, появится окно **Error Suggestions**:

* **Network Error:** Проблемы с API Cloud.ru.
* **Context Limit:** Предложение упростить промпт или разбить задачу.
* **Switch Model:** Кнопка быстрого переключения на модель `Qwen 3 Coder` (рекомендуется для сложных задач).
