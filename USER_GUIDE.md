# Руководство пользователя TestOps Evolution Forge

Добро пожаловать в **TestOps Evolution Forge** — ваш AI-ассистент для автоматической генерации тестов.

## 🚀 Как начать

1.  **Откройте приложение**
    Перейдите по адресу: [http://localhost:3000](http://localhost:3000)

    Вы увидите интерфейс **Evolution Workspace**:
    *   Слева: Панель настройки задачи.
    *   По центру: Редактор кода (здесь появится результат).
    *   Справа: Логи работы агента (его "мысли" и статус).

---

## 🧪 Сценарий 1: Генерация UI-теста (Калькулятор)

Этот сценарий идеально подходит для тестирования веб-интерфейсов. Агент автоматически создаст **Page Object Model**.

1.  **Ввод задачи**
    В левой панели в поле ввода напишите:
    ```text
    Напиши UI тест на Python (Playwright) для калькулятора Cloud.ru (https://cloud.ru/calculator).
    Сценарий:
    1. Открыть страницу.
    2. Добавить сервис 'Виртуальная машина'.
    3. Изменить количество CPU на 4.
    4. Проверить, что цена изменилась.
    ```

2.  **Запуск**
    Нажмите кнопку **"Создать тест"**.

3.  **Наблюдение**
    *   В правой панели (Terminal) начнут появляться логи: `Analyst: Parsing requirements...`, `Coder: Generating Page Object...`.
    *   Статус сменится на **Running**.

4.  **Результат**
    *   Через 15-30 секунд в центральном редакторе появится готовый код на Python.
    *   Код уже прошел проверку синтаксиса и линтеров.
    *   Вы можете скопировать его кнопкой **Copy** или скачать.

---

## 🔌 Сценарий 2: Генерация API-теста (Swagger)

Агент умеет читать документацию API и писать тесты на `pytest` + `requests`.

1.  **Ввод задачи**
    В поле ввода вставьте ссылку на Swagger или опишите эндпоинт:
    ```text
    Сгенерируй API тест для метода GET /v1/instances.
    Используй базовый URL: https://compute.api.cloud.ru
    Проверь, что в ответе приходит список машин и статус код 200.
    Добавь негативный тест на 401 (без токена).
    ```

2.  **Запуск**
    Нажмите **"Создать тест"**.

3.  **Результат**
    *   Агент сформирует структуру теста с фикстурами для авторизации (Bearer Token).
    *   Будут созданы проверки JSON-схемы ответа.

---

## ⚡ Сценарий 3: Пакетная генерация (Batch)

Хотите создать сразу много тестов? Агент умеет работать параллельно.

1.  **Ввод задачи**
    ```text
    Напиши тесты для интернет-магазина:
    ### SCENARIO: Проверка логина успешная
    ### SCENARIO: Проверка логина с неверным паролем
    ### SCENARIO: Добавление товара в корзину
    ```
    *(Используйте разделитель `### SCENARIO:` для явного указания)*.

2.  **Запуск**
    Агент распознает 3 сценария и запустит их генерацию одновременно. Результат появится быстрее, чем если бы вы делали это по очереди.

---

## 🛠 Устранение неполадок

*   **Статус "Error"**: Если агент не смог сгенерировать валидный код с 3-х попыток, он вернет последнюю версию с комментарием об ошибке. Попробуйте уточнить промпт.
*   **Зависло на "Processing"**: Проверьте, запущен ли Docker контейнер `backend`.
