# Руководство пользователя TestOps Evolution Forge

Добро пожаловать в **TestOps Evolution Forge** — автономную IDE для QA-инженеров. Этот гид поможет вам эффективно использовать возможности мульти-агентной системы.

## 🖥 Интерфейс

Приложение разделено на три рабочие зоны:

1. **Sidebar (слева):** Чат с агентом, загрузка файлов, настройки контекста.
2. **Editor (центр):** Редактор кода (Monaco) и просмотр тест-плана.
3. **Terminal (справа):** Визуализация "мыслей" агента и логи выполнения.

---

## 💬 1. Режим Чата и Генерация

### Основы

Просто напишите задачу в чат. Агент помнит контекст всей сессии.

* **Пример:** `Протестируй форму входа на https://example.com`
* **Уточнение:** `Добавь проверку на ввод спецсимволов в поле пароля`

### ✨ AI Enhance (Улучшение промпта)

Не уверены, как правильно сформулировать задачу?

1. Напишите короткий запрос (например, "протестируй апи").
2. Нажмите иконку **Волшебной палочки (🪄)** над полем ввода.
3. Система перепишет ваш промпт, добавив структуру, шаги и требования к проверкам.

### ⚡ Parallel Batch Generation (Пакетная генерация)

Нужно сгенерировать много тестов сразу?

* **Промпт:** `Напиши отдельные тесты для: 1) Логина, 2) Регистрации, 3) Восстановления пароля`.
* **Результат:** Агент-Аналитик разобьет задачу на сценарии, и система сгенерирует код для них **параллельно**.

---

## 👁️ 2. UI Тестирование (Active Vision)

Чтобы тесты были надежными и не падали на динамических локаторах:

1. **Всегда указывайте URL** в промпте.
2. Система запустит **Web Inspector**:
   * Зайдет на сайт в headless-режиме.
   * Скачает "слепок" DOM-дерева.
   * Найдет надежные атрибуты (`data-testid`, `id`, семантические классы).
3. **Результат:** Тест будет использовать реальные селекторы, а не галлюцинации.

---

## 📦 3. API Тестирование (White-Box Analysis)

Система умеет читать код вашего проекта для написания точных интеграционных тестов.

### Способ А: Загрузка архива

1. Нажмите иконку **Скрепки (📎)**.
2. Загрузите `.zip` архив с исходным кодом.

### Способ Б: Git Repository

1. Нажмите иконку **Git Branch**.
2. Введите URL репозитория (поддерживаются публичные и приватные с токеном).
3. Нажмите **Analyze**.

**Что происходит:** Агент просканирует код (Python FastAPI, Java Spring, Node.js), построит карту API-эндпоинтов и предложит тесты на основе реальной сигнатуры методов.

---

## 🔧 4. Запуск и Отладка (Execution & Debugging)

### Запуск тестов

1. Нажмите зеленую кнопку **▶ Run Test** в верхней панели редактора.
2. Тест запустится в изолированном Docker-контейнере.
3. После завершения станет доступна вкладка **External Link (Allure Report)** с красивым отчетом.

### 🕵️‍♂️ Debug Inspector (Если тест упал)

Если тест завершился ошибкой (`FAILURE`), рядом со статусом в Истории появится иконка **Жука (🐞)**. Нажмите её, чтобы открыть **Debug Report**:

* **Hypothesis:** Что агент думает о причине падения.
* **DOM Snapshot:** Как выглядела страница в момент ошибки (интерактивный просмотр HTML).
* **Network & Console:** Были ли 500-е ошибки API или JS-ошибки в консоли.

### 🛠 Auto-Fix with AI

В редакторе кода появится всплывающая кнопка **Auto-Fix**.

1. Нажмите её.
2. Система отправит логи, трейсы и код агенту-отладчику (`Debugger Agent`).
3. Агент перепишет код, исправив локаторы или логику ожидания.

---

## ⏰ 5. Автономное Обслуживание (Scheduler)

Вам не нужно делать это вручную. Система работает в фоне:

* **Каждые 6 часов** запускается проверка (Health Check) для успешных тестов.
* Если тест перестал проходить (изменилась верстка), система **сама** запустит процедуру Auto-Fix.
* Вы увидите уведомление в интерфейсе: *"Test for '...' was automatically repaired"*.

---

## ⚙️ 6. Настройки и Модели

Нажмите иконку **Шестеренки (⚙️)** в панели Терминала:

* **AI Models:** Переключайтесь между моделями (Qwen 3 Coder, GigaChat, T-Pro, GPT-4o). Если одна модель не справляется, попробуйте другую (например, Qwen 3 Coder лучше всего пишет код).
* **Editor:** Настройте размер шрифта и миникарту.
* **Account:** Скопируйте свой `Session UUID`, чтобы открыть ту же сессию на другом устройстве.

---

## 🚀 7. Экспорт

Когда тест готов:

1. Нажмите кнопку **GitMerge (Export)**.
2. Введите данные GitLab (Project ID, Token).
3. Система создаст новую ветку и **Merge Request** в вашем репозитории.
